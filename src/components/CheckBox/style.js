import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  items: {
    flexDirection: 'row',
    marginTop: 10,
    width: '50%',
    alignItems: 'center',
  },
  checkbox: {
    width: 26,
    height: 26,
  },
});

export default styles;
