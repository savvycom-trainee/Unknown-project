import { Colors } from '../../themes';

const styles = {
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: Colors.default,
    height: 40,
    width: 40,
    borderRadius: 20,
    position: 'absolute',
  },
};

export default styles;
