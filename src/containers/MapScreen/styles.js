import * as d from '../../utilities/Tranform';

const styles = {
  centerHeaderStyle: {
    fontSize: 15,
    fontWeight: '600',
  },
  flatListStyle: {
    position: 'absolute',
    bottom: 25 * d.ratioH,
    paddingLeft: 40 * d.ratioW,
    left: 0,
    right: 0,
  },
};

export default styles;
